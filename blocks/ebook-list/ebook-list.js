import decorate from '../resource-list/resource-list.js';

/**
 * Compatibility entry point for pages that still author the legacy block name.
 * @param {Element} block The legacy ebook-list block element
 */
export default decorate;
